---
export type Heading = {
  subheadings: Heading[];
  depth: number;
  slug: string;
  text: string;
};

interface Props {
  heading: Heading;
}
const { heading } = Astro.props;
---

<li class="brand-accent-hover-strong">
  <a href={"#" + heading.slug} class="text-gray-600 dark:text-gray-400 brand-accent-hover transition-colors">
    {heading.text}
  </a>
  {
    heading.subheadings.length > 0 && (
      <ul class="pl-5">
        {heading.subheadings.map((subheading) => (
          <Astro.self heading={subheading} />
        ))}
      </ul>
    )
  }
</li>
