---
import "@styles/global.css";
import "@fontsource-variable/bricolage-grotesque";
import Header from "@components/common/Header.astro";
import SEO from "@components/common/SEO.astro";
import { withBase } from "@utils/slug";
import { siteConfig } from '@config/site';
import Footer from "@components/common/Footer.astro";

interface Props {
  title?: string;
  description?: string;
  image?: string;
  indexable?: boolean;
  wideContent?: boolean;
  article?: boolean;
  canonical?: string;
  publishDate?: Date;
  modifiedDate?: Date;
  author?: {
    name: string;
    url?: string;
  };
  tags?: string[];
  categories?: string[];
  breadcrumbs?: Array<{ name: string; url: string }>;
  video?: {
    src: string;
    name?: string;
    description?: string;
    thumbnailUrl?: string;
    uploadDate?: Date;
    duration?: string;
  };
}

const { 
  title = siteConfig.title,
  description = siteConfig.description,
  image = siteConfig.ogImage,
  indexable = true,
  wideContent = false,
  article = false,
  canonical,
  publishDate,
  modifiedDate,
  author,
  tags,
  categories,
  breadcrumbs,
  video,
} = Astro.props;
---

<!DOCTYPE html>
<html lang={siteConfig.lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href={withBase(siteConfig.favicons.svg)} />
    <link rel="icon" type="image/x-icon" href={withBase(siteConfig.favicons.ico)} />
   
   
    <meta name="generator" content={Astro.generator} />
    <script is:inline>
      (() => {
        let savedTheme = null;
        try {
          savedTheme = localStorage.getItem('theme');
        } catch {
          /* Storage blocked (private mode, sandboxed frame): fall through to
             the OS preference so first paint still picks a sane theme. */
        }
        const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        document.documentElement.classList.toggle('dark', savedTheme === 'dark' || (!savedTheme && prefersDark));
      })();
    </script>
    
    <!-- Critical CSS -->
    <style is:inline>
      body { margin: 0; display: flex; flex-direction: column; min-height: 100vh; }
      .dark body { background-color: #111827; color: #fff; }
      main { flex: 1 0 auto; }
    </style>
    
    <!-- SEO Component -->
    <SEO
      title={title}
      description={description}
      image={image}
      article={article}
      indexable={indexable}
      canonical={canonical}
      publishDate={publishDate}
      modifiedDate={modifiedDate}
      author={author}
      tags={tags}
      categories={categories}
      breadcrumbs={breadcrumbs}
      video={video}
    />
  
  
    
  </head>
  <body class="bg-white dark:bg-gray-900 text-gray-900 dark:text-white min-h-screen flex flex-col">
    <a href="#main-content" class="skip-link">Skip to content</a>
    <Header />
    
    <!-- Main content area with conditional width -->
    <main id="main-content" class="grow w-full" tabindex="-1">
      <div class={wideContent ? "w-full" : "max-w-5xl mx-auto px-4 sm:px-6 lg:px-8"}>
        <slot />
      </div>
    </main>
    
    <!-- Full-width footer -->
    <Footer />
    <script>
      const syncThemeToggles = () => {
        const isDark = document.documentElement.classList.contains('dark');
        document.querySelectorAll<HTMLElement>('[data-theme-toggle]').forEach((toggle) => {
          toggle.setAttribute('aria-pressed', String(isDark));
          toggle.setAttribute('aria-label', 'Switch to ' + (isDark ? 'light' : 'dark') + ' mode');
        });
      };

      syncThemeToggles();

      document.addEventListener('click', (event) => {
        const toggle = event.target instanceof Element ? event.target.closest('[data-theme-toggle]') : null;
        if (!toggle) return;

        const isDark = !document.documentElement.classList.contains('dark');
        document.documentElement.classList.toggle('dark', isDark);
        try {
          localStorage.setItem('theme', isDark ? 'dark' : 'light');
        } catch {
          /* Storage blocked: the toggle still works for this page view, it
             just won't persist across reloads. */
        }
        syncThemeToggles();
      });
    </script>
    <style is:global>
      .skip-link {
        position: fixed;
        top: 0.5rem;
        left: 0.5rem;
        z-index: 100;
        transform: translateY(-150%);
        border-radius: 0.375rem;
        background: #fff;
        color: #111827;
        padding: 0.75rem 1rem;
        font-weight: 600;
        transition: transform 150ms ease;
      }

      .skip-link:focus {
        transform: translateY(0);
      }
    </style>
  </body>
</html>
